[plugin]
name="MCP Bridge"
description="WebSocket bridge for Claude Code MCP integration. Exposes editor operations (read/write files, run scenes, get state) to external tools via JSON-RPC over WebSocket."
author="Claude Code Game Studios"
version="0.1.0"
script="mcp_bridge.gd"
